import { CircularProgress } from "@mui/material";

const Loader = () => {

  return <center style={{marginTop:'10%'}}>
    <CircularProgress color="primary" size={200} />;
  </center>
};

export default Loader;
